{
  "name": "rhumint-central-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest",
    "db:migrate:local": "wrangler d1 migrations apply rhumint-central --local",
    "db:migrate:remote": "wrangler d1 migrations apply rhumint-central --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
