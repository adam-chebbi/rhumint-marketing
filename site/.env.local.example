# Admin password for the admin panel login page.
ADMIN_PASSWORD=change-me

# Central API URL (the Cloudflare Worker).
CENTRAL_API_URL=http://localhost:8787

# Admin API key (must match ADMIN_API_KEY in central-api Worker secrets).
CENTRAL_API_KEY=change-me-too
